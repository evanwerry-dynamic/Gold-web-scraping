# Dashboard server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-dotenv>=1.0

# Bot data feeds & math (paper trading)
websockets>=12.0
requests>=2.0
numpy>=1.24.0
scipy>=1.10.0
anthropic>=0.25.0

# Persistence
psycopg2-binary>=2.9.0

# Polymarket CLOB client
py_clob_client_v2>=1.0.1

# On-chain wallet: balance checks, pUSD approval, conditional-token redemption (live mode)
web3>=6.0.0

# Tests (pytest.ini sets asyncio_mode=auto)
pytest>=8.0.0
pytest-asyncio>=0.23.0
